# 科研工作台需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 版本 | v0.1 |
| 日期 | 2026-08-10 |
| 状态 | 需求讨论完成，待开发排期 |
| 前置代码 | react-note（前端）/ fastapi-note-server（后端） |

---

## 1. 背景与目标

用户为计算机专业研究生，阅读文献、记笔记是日常核心工作，最终目标服务论文写作。现有工具链割裂：文献阅读、笔记、AI 问答相互独立，笔记无法关联文献，写作时整理参考文献费时费力。

已有 `react-note`（React 19 + Tiptap 3 + Dexie 离线笔记）与 `fastapi-note-server`（FastAPI + DeepSeek 流式中转）作为基础代码，并已掌握 LangChain / RAG 技术。希望通过**最小化改动**将现有项目改造为一个一体化、本地优先的个人科研工作台。

**核心痛点：**
1. 文献与笔记分离，读完就忘，想法无法关联到具体文献
2. 写作时参考文献整理（格式、编号、导出 Word）耗时
3. 提问 AI 时上下文割裂，无法结合"正在读的文献"和"自己的笔记"

## 2. 产品定位

> **本地优先的个人学术文献工作台**：文献管理 → PDF 阅读 → 笔记+引用 → AI 问答 → 论文写作导出，一条链路打通。数据以普通文件形式保存在用户自选的本地文件夹（vault）中，可被 Obsidian / VS Code / Git / 云盘直接使用。

## 3. 目标用户与核心场景

- 主用户：本人（计算机专业研究生，单机使用）
- 场景 1 **导入文献**：拖入 PDF（arXiv / 期刊）→ 自动补全元数据 → 归入集合（研究项目）
- 场景 2 **阅读**：离线打开 PDF，选中段落 → 一键"转笔记（自动带引用）"或"问 AI（解释 / 翻译 / 总结）"
- 场景 3 **记笔记**：阅读中随手记录，随时引用文献；引用反向可见（这篇文献被哪些笔记引用）
- 场景 4 **写作**：按集合/笔记聚合生成参考文献列表（GB/T 7714 / APA / IEEE 一键切换）→ 导出 Word；导出 BibTeX 供 Overleaf / Zotero 使用
- 场景 5 **提问**：随时向 AI 提问，可限定"当前这篇文献"或"整个知识库（笔记+文献）"

## 4. 功能需求

优先级定义：**P0** = MVP 必须；**P1** = 第一轮增强；**P2** = 远期。

### 4.1 工作台与 vault 管理（P0）

- 首次启动引导选择/创建工作台文件夹（vault），记住设置
- vault 目录约定见 5.3；应用启动时扫描 vault 重建索引
- 外部修改感知：vault 文件被 Obsidian / VS Code 改动后能感知（轻量轮询或后端 watchdog + SSE 通知前端刷新）
- 存储层抽象 `StorageAdapter`：开发期（浏览器）与发布期（Tauri 原生 fs）共用一套业务代码
- 开发期浏览器模式使用 File System Access API（或后端文件 API 兜底）

### 4.2 文献管理（P0）

- **导入 PDF**：文件选择 / 拖拽导入 → 复制到 `vault/文献/` → 触发元数据补全
- **元数据字段**：标题、作者、年份、期刊/会议、卷期页码、DOI、arXiv ID、关键词、阅读状态（未读/在读/已读）、标签、所属集合
- **元数据获取**：手动录入 + 输入 DOI / arXiv ID 自动补全（Crossref API / arXiv API，均免费）【P0】；BibTeX 文件导入【P1】
- **重复检测**：DOI 精确匹配【P0】，标题模糊匹配【P1】
- **列表视图**：搜索（标题/作者）、按集合/阅读状态/标签过滤、按添加时间/年份排序
- **删除**：从 vault 删除 PDF 并清理索引与引用

### 4.3 PDF 阅读器（P0）

- 基于 Mozilla pdf.js 自研组件：缩放、翻页、页码跳转、目录、文本搜索
- **划词操作（核心）**：选中文字弹出操作菜单 →
  1. **转笔记引用**：新建/追加到笔记，自动生成引用标记（含页码、原文摘录）
  2. **问 AI**：解释 / 翻译 / 总结选中内容
  3. 复制
- PDF 二进制存于 vault，离线可读
- 高亮 / 批注 / 划线【P1，v1.1】：存 `.kb/annotations.json`
- 阅读进度记忆【P1】：重开时回到上次阅读位置

### 4.4 笔记系统（P0）

- **文件格式**：Markdown + YAML frontmatter（`title` / `collection` / `tags` / `cites`）
- 编辑器沿用 Tiptap 3，补充 Markdown 序列化（自定义节点：引用徽章、背景色需写序列化扩展）
- 自动保存沿用现有 600ms 防抖，直接写 .md 文件
- 笔记列表：扫描 `vault/笔记/`，集合分组、搜索、新建/重命名/删除
- 删除笔记时同步清理后端索引与引用

### 4.5 引用系统（P0）

- **引用标记**：笔记内行内标记（如 `[[cite:文献ID]]`），渲染为"作者+年份"徽章，点击跳转文献详情；Markdown 源文件保持可读
- **单向链接**：笔记 → 文献；**反向视图**：文献详情显示"被哪些笔记引用"，形成写作素材地图
- **参考文献列表**：按笔记或集合聚合所有引用，按 GB/T 7714（默认）/ APA / IEEE 格式化，编号自动重排
- **数据与格式分层**：只存结构化元数据，格式化是导出时的纯函数，格式切换不改数据
- 引用带页码与摘录【P1】：划词转笔记时记录，写作时引用更准确

### 4.6 AI 助手（P0）

- 入口沿用现有三栏布局中的 AI 面板（流式 SSE 打字机体验不变）
- **能力分层**：
  1. **全局问答**：RAG 检索笔记 + 文献文本，回答附来源（文献名/页码）
  2. **单篇阅读问答**：以当前 PDF 为上下文（限定该文献的向量检索）
  3. **划词问答**：解释 / 翻译 / 总结选中内容
  4. **任务模式**：总结 / 润色 / 续写（沿用现有）
- **多文献对比 / 综述辅助**（P1）："这 N 篇的方法/数据集/结论给我对比表"、研究方向脉络梳理
- **引用推荐**（P2）：根据笔记内容推荐应引用文献

### 4.7 导出（P0 / P1）

- **参考文献列表 → Word（.docx）**【P0】：python-docx 后端生成，GB/T 7714 / APA / IEEE 切换
- **BibTeX 导出**【P0】：供 Zotero / Overleaf 使用
- **笔记导出** Markdown / Word【P1】

## 5. 技术架构

### 5.1 总体架构

```
┌───────────────────────────── 本机 ─────────────────────────────┐
│                                                               │
│  前端（React 19 + Vite + Tiptap 3 + pdf.js + Tailwind/shadcn）  │
│  │  StorageAdapter（开发:浏览器FS / 发布:Tauri fs）             │
│  │                           │                                 │
│  ▼                           ▼                                 │
│  vault 文件夹 ──────────► 后端（FastAPI + LangChain RAG）        │
│  （笔记.md / 文献.pdf / .kb）│  ChromaDB + SiliconFlow embedding │
│                             │  DeepSeek chat（SSE 流式，协议不变）│
│                             │  python-docx / BibTeX 导出        │
└───────────────────────────────────────────────────────────────┘
```

- 开发期：Vite dev + `uvicorn app.main:app --port 3001`，浏览器访问
- 发布期：Tauri 2 壳加载前端构建产物 + 原生 fs 插件；后端 venv + `start.bat` 一键启动（PyInstaller 打包后置）

### 5.2 关键架构决策

| 决策点 | 结论 | 理由 |
|---|---|---|
| 数据存储 | **本地文件 vault，弃用 IndexedDB** | 数据主权、Obsidian/VS Code/Git/云盘兼容 |
| 运行形态 | 开发期浏览器 + 发布期 **Tauri 2** | 轻量原生体验；Electron 过重 |
| 笔记格式 | **Markdown + frontmatter** | 通用可读、可被其他工具打开 |
| PDF 阅读器 | **pdf.js 自研，划词引用/提问** | 工作台灵魂动作；高亮批注后置 |
| Embedding | **SiliconFlow API（BAAI/bge-m3）** | DeepSeek 无 embedding 接口；OpenAI 兼容协议复用现有 openai 包 |
| 向量库 | **ChromaDB** | LangChain 一等公民、按元数据过滤删除 |
| PDF 文本抽取 | **pypdf** 起步 | 轻量；双栏乱序时升级 docling |
| LLM | **DeepSeek chat**（现有中转，协议不变） | 零改动、成本低 |
| 后端与前端的关系 | 后端直接读 vault 建索引，**无需前端推送同步** | 文件是唯一真源，架构反而简化 |

### 5.3 vault 目录结构

```
科研工作台/                  ← 用户自选文件夹
├─ 笔记/
│   ├─ 2026-08-10 多模态RAG综述.md   ← YAML frontmatter: title/collection/tags/cites
│   └─ ...
├─ 文献/
│   ├─ 2401.xxxxx.pdf
│   └─ ...
└─ .kb/                      ← 应用内部元数据
    ├─ literature.json       ← 文献元数据索引（含 PDF 路径、DOI、阅读状态）
    ├─ collections.json
    ├─ annotations.json      ← 高亮批注（v1.1）
    ├─ settings.json
    └─ chroma_db/            ← 向量库（可由后端重建）
```

### 5.4 数据模型

- **literature.json 条目**：`id, title, authors[], year, venue, volume, issue, pages, doi, arxivId, bibtexKey, pdfPath, status, collectionIds[], tags[], importedAt`
- **笔记 frontmatter**：`title, collection, tags, cites[]`
- **后端向量库 chunk 元数据**：`docType(note|paper), docId, page(可选), title`

### 5.5 后端 API 增量清单

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/documents` | 导入 PDF（文件流或 vault 内路径），触发元数据补全 + 索引 |
| GET | `/api/documents` | 文献列表（或前端直接读 literature.json） |
| DELETE | `/api/documents/{id}` | 删除文献 + 清理索引 |
| POST | `/api/kb/refresh` | 全量/增量重建向量索引（启动时自动执行） |
| GET | `/api/kb/status` | 索引状态（chunk 数、未索引文件） |
| POST | `/api/chat` | **沿用现有协议**；对话模式增加 RAG 检索注入，SSE 格式不变 |
| POST | `/api/export/references` | 按集合/笔记生成参考文献列表（docx / txt） |
| POST | `/api/export/bibtex` | BibTeX 导出 |

### 5.6 RAG 管线

- 分块：`RecursiveCharacterTextSplitter`，chunk 500 / overlap 50，分隔符含中文标点
- Embedding：SiliconFlow `BAAI/bge-m3`（OpenAI 兼容接口，复用现有 openai SDK）
- 向量库：ChromaDB 本地持久化
- 检索：全局 top-k=5（笔记+文献混合）；单篇模式 `where: {docId: 当前文献}` 限定
- 索引维护：启动全量扫描 + 文件变更检测（mtime 比对 / watchdog）；embedding 失败时 chat 自动降级为无 RAG
- 回答要求附来源（文献标题 + 页码），由 system prompt 约束

### 5.7 后端依赖增量

`langchain-core`、`langchain-openai`、`langchain-text-splitters`、`langchain-chroma`、`pypdf`、`beautifulsoup4`、`python-multipart`、`python-docx`（导出）

## 6. 非功能需求

- **本地优先**：阅读、笔记、检索离线可用（仅 AI 对话与元数据补全需联网）；核心数据不依赖云服务
- **隐私**：除 DeepSeek / SiliconFlow API 请求外，数据不出本机；vault 文件不加密、用户自管
- **性能**：千级文献 / 万级 chunk 检索 < 2s；PDF 打开 < 3s；编辑保存 < 1s
- **兼容**：Windows 10+；vault 文件可被 Obsidian / VS Code 直接打开编辑
- **备份**：vault 支持 Git 管理、云盘同步；`.kb/` 可由后端重建，损坏可删

## 7. 里程碑

| 阶段 | 范围 |
|---|---|
| **M1（MVP）** | vault 接入与扫描；文献导入 + 元数据补全；PDF 阅读（划词引用/提问）；笔记迁移 Markdown；引用系统（标记/列表/反向视图）；全局与单篇 RAG 问答；参考文献列表导出 docx + BibTeX |
| **M2** | 高亮/批注；划词翻译；阅读状态与进度；集合级导出；索引 watchdog；Tauri 2 壳 |
| **M3** | 多文献对比/综述；笔记导出；后端 PyInstaller 打包；全文搜索增强 |

## 8. 风险与开放问题

1. **后端 PyInstaller 打包**：chroma/langchain 含原生依赖，打包坑多 → 后置；必要时换 sqlite-vec
2. **PDF 双栏抽取乱序**（pypdf）→ 预留 docling 升级路径
3. **File System Access API 兼容性**（仅 Chromium）→ 开发期提供后端文件 API 兜底，发布期 Tauri 无此问题
4. **响应式列表**：IndexedDB 的 useLiveQuery 失效，需"本地操作即时刷新 + 轮询/watchdog SSE"方案
5. **Markdown 序列化**：Tiptap 自定义节点（引用徽章、背景色）需自写 markdown 序列化/反序列化
6. **引用格式准确性**：GB/T 7714 / APA / IEEE 的边界规则多，导出实现需对照规范逐项校验
7. **元数据补全失败兜底**：Crossref/arXiv 查不到的文献允许手动录入，且必须能编辑已有元数据

---

## 9. M1 排期与技术清单（2026-08-10）

> 本清单为 M1（MVP）的开发拆解。估时按"全职、单人、有现成代码基础"计算；若业余开发（每周约 16-20h），请按 2 倍折算排期。

### 9.1 总览

| 维度 | 估时 |
|---|---|
| 后端（B1-B8） | 8 天 |
| 前端（F1-F7） | 13.5 天 |
| 联调与回归（T1-T2） | 2.5 天 |
| 风险缓冲 | 1.5 天 |
| **合计** | **约 25.5 个工作日（全职约 5 周；业余约 8-10 周）** |

### 9.2 任务清单（WBS）

| 编号 | 任务 | 端 | 依赖 | 估时 | 技术要点 |
|---|---|---|---|---|---|
| B1 | 后端基建：依赖 + 配置 | 后端 | — | 0.5d | requirements 增量（langchain-core/openai/text-splitters/chroma、pypdf、beautifulsoup4、python-multipart、python-docx）；config.py 加 SILICONFLOW_API_KEY/BASE_URL、EMBEDDING_MODEL、KB_DIR、VAULT_PATH；更新 .env.example |
| B2 | 元数据补全服务 | 后端 | B1 | 1d | Crossref `GET api.crossref.org/works/{doi}`；arXiv `GET export.arxiv.org/api/query?id_list=...`（Atom XML）；字段规范化 + 失败兜底 |
| B3 | 知识库核心 kb.py | 后端 | B1 | 1.5d | Chroma 持久化（langchain-chroma）；OpenAIEmbeddings(base_url=SiliconFlow, model=BAAI/bge-m3)；RecursiveCharacterTextSplitter(500/50，分隔符含中文标点)；pypdf 抽取；upsert/delete/retrieve（支持 where 限定单篇） |
| B4 | vault 文件 API | 后端 | B1 | 1d | `GET/POST/PUT/DELETE /api/fs/*`（read/write/list/mkdir/delete/exists）；路径必须 resolve 后在 vault 内（防目录穿越）；开发期前端的文件读写兜底 |
| B5 | 文献导入与管理 API | 后端 | B2, B4 | 1d | `POST /api/documents`（multipart PDF → 复制到 vault/文献/ → 元数据补全 → 写 literature.json → 触发索引）；`GET /api/documents`；`DELETE /api/documents/{id}`；literature.json 读写 |
| B6 | 索引管理与变更检测 | 后端 | B3, B4 | 1d | 启动全量扫描（笔记 .md + 文献 PDF）；mtime 比对增量更新；`GET /api/kb/status`（chunk 数/未索引文件）；`POST /api/kb/refresh` |
| B7 | chat RAG 注入 | 后端 | B3 | 1d | 对话模式检索 top-k=5 注入 system prompt（全局 / 单篇 where 过滤）；SSE 格式逐字节不变；embedding 失败降级为无 RAG |
| B8 | 导出服务 | 后端 | B5 | 1.5d | 三个格式化纯函数（GB/T 7714 默认 / APA / IEEE），编号自动重排；python-docx 生成 .docx；BibTeX 序列化；`POST /api/export/references`、`POST /api/export/bibtex` |
| F1 | StorageAdapter 抽象 | 前端 | B4 | 1.5d | 接口：read/write/list/mkdir/delete/exists；HttpFsAdapter（fetch 后端）；替换 db.ts/useNotes/SiderBar 的 Dexie 调用点（接口先行，可用 mock 并行开发） |
| F2 | 笔记 Markdown 化 | 前端 | F1 | 2d | gray-matter 解析/写 frontmatter（title/collection/tags/cites）；tiptap-markdown 序列化（已有基础）+ 自定义节点（引用徽章、背景色）写 extendMarkdown；自动保存改写 .md 文件（防抖沿用）；集合迁移 collections.json |
| F3 | 侧边栏/列表文件模式改造 | 前端 | F2 | 1.5d | 数据源从 useLiveQuery 改为文件扫描 + 内存状态；操作即时刷新 + 30s 轻量轮询兜底；搜索/集合分组/拖拽/重命名/删除适配 |
| F4 | 文献库 UI | 前端 | B5, F1 | 2.5d | 文献列表（搜索/状态/集合过滤）；导入（文件选择/拖拽 → 上传）；元数据补全交互（输入 DOI/arXiv → 自动补全 → 可编辑确认）；文献详情（元数据/阅读状态/反向引用）；删除 |
| F5 | PDF 阅读器 | 前端 | F1, B5 | 3d | pdfjs-dist（注意 Vite 下 worker 用 `new URL(...pdf.worker.mjs, import.meta.url)`）；canvas 渲染 + text layer + 翻页/缩放/页码；划词浮层（getSelection + range.getBoundingClientRect 定位）；操作菜单：转笔记引用 / 问 AI / 复制 |
| F6 | 引用系统 UI | 前端 | F4, F5 | 2d | Tiptap 内联引用节点（渲染"作者+年份"徽章，点击跳文献详情）；笔记尾参考文献列表实时渲染；反向引用视图 |
| F7 | AI 面板适配 | 前端 | B7, F5 | 1d | 全局问答 / 单篇问答（传当前文献 ID 限定检索）/ 划词提问；复用现有 SSE 打字机 |
| T1 | 端到端联调 | 前后端 | 全部 | 1.5d | 导入→阅读→划词引用→笔记→全局/单篇问答→导出 docx/BibTeX 全链路 |
| T2 | 回归测试 | 前后端 | T1 | 1d | 笔记编辑/保存/删除/集合；任务模式（总结/润色/续写）SSE；断网降级 |

### 9.3 阶段排期（依赖链）

```
阶段 A ── B1 ──► 并行启动：B2 ──► B5（文献线）
                 │          B3 ──► B7（AI 线，可提前）
                 └─ B4 ──► F1（笔记线地基，接口可先 mock）
阶段 B 笔记线：F1 ──► F2 ──► F3            （关键路径①）
阶段 C 文献线：B5 ──► F4                   （与 B 并行）
阶段 D 阅读器：F4/F5 ──► F6                （关键路径②，F5 最大单项，建议最早开工）
阶段 E 导出：B8（依赖 B5，可在写作场景收尾时验收）
阶段 F 收尾：T1 ──► T2
```

- **关键路径**：B1 → B4 → F1 → F2 → F3 → F5 → F6 → T1，约 13 天，决定总工期
- **可并行**：B2/B3/B4 三线并行；B 线与 C 线并行；B8 与前端后半程并行
- **建议最早开工**：F5（PDF 阅读器）不确定性最大，其文件读取部分可先用 mock 数据开发

### 9.4 技术清单速查

**前端：**
- pdfjs-dist + Vite：worker 必须 `new Worker(new URL('pdfjs-dist/build/pdf.worker.mjs', import.meta.url), {type:'module'})`，否则构建报错
- 划词浮层：`document.getSelection()` + `range.getBoundingClientRect()`；渲染用现有 shadcn popover 类组件
- 引用标记：Tiptap Inline node（`data-cite` 属性），markdown 序列化为 `[[cite:id]]`（tiptap-markdown 的 extendMarkdown 扩展）
- frontmatter：gray-matter；后端索引侧用 pyyaml 解析同一文件
- 文件读写：HttpFsAdapter（开发期）→ @tauri-apps/plugin-fs（发布期，M2），业务代码零改动
- 列表刷新：本地操作即时更新 + 30s 轮询（M2 换 watchdog + SSE）
- 状态：Zustand 扩展 vault/literature store；AIPanel 打字机逻辑原样复用

**后端：**
- Embedding：OpenAIEmbeddings 指向 SiliconFlow base_url，模型 `BAAI/bge-m3`（OpenAI 兼容，复用现有 openai 包，无需新 SDK）
- 分块：RecursiveCharacterTextSplitter，separators 含 `。！？；，` 等中文标点，chunk 500 / overlap 50
- 向量库：Chroma 本地持久化到 `.kb/chroma_db`，chunk 元数据 `docType(note|paper)/docId/page/title`
- PDF 抽取：pypdf；保留 docling 升级接口（双栏乱序时替换抽取函数即可）
- 引用格式：三个纯函数格式化器（GB/T 7714 / APA / IEEE），数据层不分格式
- 文件 API 安全：`Path.resolve()` 后必须 `is_relative_to(VAULT)`，防目录穿越
- 索引：mtime + 大小比对做增量，`literature.json` 为权威元数据

### 9.5 M1 完成定义（验收标准）

1. 笔记、PDF、元数据均为 vault 内普通文件，可被 Obsidian / VS Code 直接打开编辑
2. 导入 PDF → DOI/arXiv 自动补全元数据（失败可手动）→ 文献列表可见可删
3. 阅读器打开 PDF：划词 → 转笔记引用（含页码/摘录）与问 AI 均可用
4. 笔记可插入/渲染引用徽章，笔记尾部显示参考文献列表，格式可切换
5. 全局问答可引用笔记+文献内容回答并附来源；单篇问答限定当前文献
6. 参考文献列表导出 .docx（GB/T 7714）与 BibTeX 成功
7. 原功能不回归：笔记编辑/保存/删除、任务模式对话、SSE 打字机
8. 开发期一键盘起（vite dev + uvicorn）；断网时核心读写不报错

### 9.6 工期建议与风险缓冲

| 风险 | 应对 | 缓冲 |
|---|---|---|
| F5 pdf.js + text layer 集成超预期（最大不确定项） | 开工最早、mock 先行；text layer 失败时先降级为"无划词的阅读器"，划词后置一天补上 | +0.5d |
| GB/T 7714 / APA 边界规则多 | 首版覆盖常见类型（期刊/会议/学位论文/书籍/网页），疑难条目手动模板 | +0.5d |
| 元数据 API（Crossref/arXiv）偶发限流或字段缺失 | B2 内置手动兜底 + 缓存 | +0.5d |
| F2 自定义节点 markdown 序列化细节多 | 引用节点优先，背景色节点可临时降级为纯文本 | — |
